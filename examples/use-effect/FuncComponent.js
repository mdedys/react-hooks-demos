import React from "react"
import Input from "../../components/input"

/**
 * Part 1: Using the input and its hook connect the document title to the input changing text
 * Part 2: Build a useEffect that fetch API data
 * Part 3: Extract API call into a custom hook
 * Part 4: Base a effect on a button value change and show how the dependecy works with multiple state values changing
 */

const FuncComponent = () => {
  return <Input />
}

export default FuncComponent
